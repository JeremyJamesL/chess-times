/** @type {import('tailwindcss').Config} */
const config = {
  content: ["./index.html", "./src/index.js"],
  theme: {
    fontFamily: {
      body: ["Lato"],
    },
    extend: {},
  },
  plugins: [],
};

export default config;
